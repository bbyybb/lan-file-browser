**中文** | [English](GUIDE_EN.md)

# 功能详细使用指南

> 返回 [README](../README.md) | 完全零基础？请看 [保姆级安装教程](BEGINNER.md)

---

## 目录

- [交互式启动引导](#交互式启动引导)
- [目录白名单（沙箱模式）](#目录白名单沙箱模式)
- [只读模式](#只读模式)
- [多用户多密码](#多用户多密码)
- [密码保护与登录](#密码保护与登录)
- [二维码快速连接](#二维码快速连接)
- [浏览文件](#浏览文件)
- [文件名搜索](#文件名搜索)
- [文件内容搜索](#文件内容搜索)
- [正则搜索](#正则搜索)
- [预览文件](#预览文件)
- [Office 在线预览](#office-在线预览)
- [视频字幕](#视频字幕)
- [ZIP 预览与解压](#zip-预览与解压)
- [在线编辑文件](#在线编辑文件)
- [下载文件](#下载文件)
- [文件夹整体下载](#文件夹整体下载)
- [上传文件与拖拽上传](#上传文件与拖拽上传)
- [文件复制](#文件复制)
- [文件移动](#文件移动)
- [新建文件夹 / 文件](#新建文件夹--文件)
- [重命名](#重命名)
- [删除文件](#删除文件)
- [临时分享链接](#临时分享链接)
- [共享剪贴板](#共享剪贴板)
- [常用目录收藏](#常用目录收藏)
- [排序与筛选](#排序与筛选)
- [亮色 / 暗色主题](#亮色--暗色主题)
- [网格 / 列表视图](#网格--列表视图)
- [中英语言切换](#中英语言切换)
- [阻止系统睡眠](#阻止系统睡眠)
- [公网访问（外网穿透）](#公网访问外网穿透)
- [访问日志](#访问日志)
- [支持的文件类型](#支持的文件类型)
- [技术架构](#技术架构)

---

## 交互式启动引导

直接运行 `python file_browser.py`（不带任何参数）时，程序会进入交互式引导，逐步配置启动选项。每个问题直接按回车即使用括号内的默认值。

```
==================================================
  LAN File Browser - 启动配置
  直接按回车使用 [默认值]
==================================================

  端口 [25600]:                        ← 回车用 25600，或输入其他端口号

  密码模式:
    1. 自动生成随机密码（默认）
    2. 自定义固定密码
    3. 不设密码
    4. 多用户多密码
  请选择 [1]:                          ← 输入数字选择，回车用默认

  访问范围:
    1. 不限制，可访问所有文件（默认）
    2. 只允许访问指定目录
  请选择 [1]:                          ← 选 2 后输入目录路径（逗号分隔多个）

  阻止系统睡眠（服务运行期间防止电脑休眠）:
    1. 是，阻止睡眠（默认）
    2. 否，允许正常睡眠
  请选择 [1]:
```

**跳过引导的三种方式：**

| 方式 | 命令 | 说明 |
|------|------|------|
| 全用默认值 | `python file_browser.py -y` | 跳过所有问题直接启动 |
| 指定参数 | `python file_browser.py --roots D:/shared` | 传了参数自动跳过引导 |
| 组合参数 | `python file_browser.py --port 8080 --password abc -y` | 指定参数 + 强制跳过 |

---

## 目录白名单（沙箱模式）

默认情况下，程序允许访问电脑上的所有文件。如需限制可访问范围，有两种方式：

**方式一：命令行参数（推荐，临时生效）**

```bash
python file_browser.py --roots D:/shared-files E:/projects

# macOS 示例
python3 file_browser.py --roots /Users/bob/Public /tmp/shared
```

**方式二：修改配置文件（永久生效）**

修改 `file_browser.py` 顶部：

```python
ALLOWED_ROOTS = ["D:/shared-files", "E:/projects"]
```

配置后的效果：
- 首页只显示白名单中的目录（替代原来的磁盘列表）
- 所有 API 自动受限：浏览、搜索、下载、上传、复制、移动等操作均只能在白名单范围内进行
- 白名单外的路径一律拒绝访问（返回 404）
- 通过符号链接（symlink）绕过的尝试也会被拦截（使用 `os.path.realpath` 解析真实路径）

启动后终端会显示：
```
  Access:   2 allowed dir(s):
            - D:/shared-files
            - E:/projects
```

不指定 `--roots` 且 `ALLOWED_ROOTS = []` 则不限制（默认行为）。

---

## 只读模式

启用后，所有用户只能浏览、预览和下载文件，无法进行任何修改操作。适合教师分享课件、公司共享文档等场景。

**禁用的操作：** 上传、新建文件/文件夹、编辑、删除、重命名、复制、移动、解压

**保留的操作：** 浏览目录、文件预览、单文件下载、批量下载、文件夹下载、搜索、收藏、剪贴板读取、分享链接

**启用方式：**

方式一：启动时交互选择
```
  权限模式:
    1. 完全权限（默认）
    2. 只读模式
  请选择 [1]: 2
```

方式二：命令行参数
```bash
python file_browser.py --read-only
```

方式三：修改配置文件
```python
READ_ONLY = True
```

**典型教师场景：**
```bash
# 只分享课件目录，只读模式，无需密码（教室内网）
python file_browser.py --roots D:/课件 --read-only --no-password
```

启动后终端会显示 `Mode: READ-ONLY`，页面上写操作按钮自动隐藏。

---

## 多用户多密码

当需要不同用户有不同权限时（如教师管理 + 学生只读），可以配置多用户模式。

在 `file_browser.py` 顶部配置：

```python
USERS = {
    "teacher": {"password": "teacher123", "role": "admin"},
    "student": {"password": "stu456",     "role": "readonly"},
}
```

**角色说明：**

| 角色 | 权限 |
|------|------|
| `admin` | 完全权限（浏览、预览、下载、上传、编辑、删除、复制、移动等） |
| `readonly` | 只读权限（浏览、预览、下载、搜索，禁止任何修改操作） |

**行为：**
- 配置 `USERS` 后自动启用多用户模式，`PASSWORD` 配置项被忽略
- 登录页面输入对应用户的密码即可，无需输入用户名（系统自动匹配）
- 只读用户登录后，页面上的写操作按钮自动隐藏
- 终端 banner 正常显示，不会暴露用户密码

**典型教师场景：**
```python
# 教师用 teacher123 登录可管理文件
# 学生用 stu456 登录只能看和下载
USERS = {
    "teacher": {"password": "teacher123", "role": "admin"},
    "student": {"password": "stu456",     "role": "readonly"},
}
ALLOWED_ROOTS = ["D:/课件"]  # 只暴露课件目录
```

> 注意：多用户模式和单密码模式（`PASSWORD`）互斥。配置了 `USERS` 后 `PASSWORD` 不生效。

---

## 密码保护与登录

启动时自动生成 32 位高强度随机密码，在终端中显示。用户在浏览器输入密码后通过 cookie 认证，关闭浏览器后需重新登录。

三种密码模式（修改 `file_browser.py` 顶部的 `PASSWORD` 变量）：

| 设置值 | 行为 |
|--------|------|
| `PASSWORD = None` | **默认**。每次启动随机生成 |
| `PASSWORD = "my123"` | 固定密码 |
| `PASSWORD = ""` | 禁用密码保护 |

---

## 二维码快速连接

登录页自动显示当前访问地址的二维码，手机扫码直接打开，无需手动输入 IP。

---

## 浏览文件

- **首页**：显示所有磁盘（Windows: C:\、D:\；macOS: /）
- **逐级浏览**：点击文件夹进入
- **面包屑导航**：顶部路径条，点击任意层级跳转（如 🏠 根 > C: > Users > Documents）

---

## 文件名搜索

1. 在顶部搜索框输入关键词，500ms 后自动搜索
2. 不区分大小写，支持部分匹配
3. 范围：当前目录递归 6 层，最多 100 条
4. 点击 ✕ 清除搜索

**技巧**：先进入目标目录再搜索，范围更小、速度更快。

---

## 文件内容搜索

1. 输入关键词后，搜索框下方出现 **"文件名搜索"** / **"内容搜索"** 切换按钮
2. 点击 **"内容搜索"** 搜索文件内部文字
3. 结果显示：文件名 + 匹配行号 + 行内容

限制：仅搜索文本/代码/Markdown 文件，单文件 512KB 以内，最多扫描 500 个文件。

---

## 正则搜索

搜索框右侧有一个 **`.*`** 按钮，点击启用正则表达式模式（高亮表示已启用）。

支持标准正则语法，例如：
- `\.py$` — 匹配以 .py 结尾的文件名
- `TODO|FIXME` — 匹配 TODO 或 FIXME
- `\d{4}-\d{2}-\d{2}` — 匹配日期格式

文件名搜索和内容搜索均支持正则模式。

---

## 预览文件

点击文件打开预览弹窗：

| 类型 | 支持格式 |
|------|---------|
| 图片 | jpg, png, gif, webp, svg, bmp, ico |
| 视频 | mp4, webm（自动加载同名字幕，其他格式取决于浏览器） |
| 音频 | mp3, wav, ogg, flac, aac |
| PDF | 浏览器内嵌阅读器（手机端提供"在浏览器中打开"按钮） |
| Markdown | GFM 渲染 + Mermaid 图表 + 文档内链接跳转 |
| **Office** | docx 渲染为 HTML、xlsx/xls 渲染为表格 |
| 代码/文本 | 40+ 种语言（py, js, ts, go, java, c, cpp 等） |
| ZIP | 查看内容列表 + 在线解压（见下方） |

弹窗操作：下载 / 分享 / 编辑 / ESC 关闭。

---

## Office 在线预览

点击 Office 文件打开预览弹窗：

<!-- Office 预览截图 -->
<p align="center">
  <img src="screenshot_office_docx_cn.png" width="380" alt="Word 文档预览">
  <img src="screenshot_office_xlsx_cn.png" width="380" alt="Excel 表格预览">
</p>

| 格式 | 预览方式 | 使用技术 |
|------|---------|---------|
| `.docx` | 渲染为 HTML 格式化文档 | mammoth.js（CDN） |
| `.xlsx` `.xls` | 渲染为 HTML 表格，多 Sheet 逐个显示 | SheetJS（CDN） |
| `.pptx` 等 | 暂不支持预览，提供下载按钮 | - |

> Office 预览需要网络访问 `cdn.jsdelivr.net` 加载 JS 库。离线环境下会回退为"不支持预览"。

---

## 视频字幕

播放视频时，程序会自动检测同目录下是否有与视频同名的字幕文件：

- 支持格式：`.vtt`、`.srt`、`.ass`
- 例如：播放 `movie.mp4` 时自动查找 `movie.vtt`、`movie.srt`、`movie.ass`
- 找到即自动加载，第一个字幕默认启用
- 如果有多种格式的字幕同时存在，播放器中可以切换

> `.vtt` 格式兼容性最好，建议优先使用。

---

## ZIP 预览与解压

点击 `.zip` 文件打开预览弹窗，可以：
- 查看 ZIP 内的完整文件列表和大小
- 点击 **"解压到此处"** 按钮将内容解压到 ZIP 所在目录

> 其他压缩格式（.rar, .7z 等）不支持预览，需下载后本地解压。

---

## 在线编辑文件

1. 点击文本/代码/Markdown 文件打开预览
2. 点击 **"✏ 编辑"** 进入编辑模式
3. 修改后点击 **"💾 保存"** 或按 **Ctrl+S**（macOS: Cmd+S）
4. 每次保存自动创建 `.bak` 备份文件

支持 Tab 缩进、修改状态指示、未保存关闭提醒。

---

## 下载文件

| 方式 | 操作 |
|------|------|
| 单文件 | 列表中点击 ⬇ 按钮，或预览弹窗中点击"下载" |
| 批量下载 | 工具栏点击"☐ 多选" → 勾选文件（或点"☑ 全选"一键选中全部文件）→ "📦 打包下载" |
| 批量删除 | 多选模式下 → "🗑 批量删除" → 确认弹窗 |
| 批量移动 | 多选模式下 → "✂ 批量移动" → 选择目标目录 |
| 批量复制 | 多选模式下 → "📋 批量复制" → 选择目标目录 |

---

## 文件夹整体下载

点击文件夹右侧的 **⬇** 按钮，整个文件夹会被递归打包为 `.zip` 下载。

- 保留内部目录结构

---

## 上传文件与拖拽上传

**方式一：按钮上传**
1. 进入目标目录 → 点击工具栏 **"⬆ 上传"**
2. 选择文件（支持多选）→ 开始上传

**方式二：拖拽上传**
直接将文件从电脑桌面或文件管理器**拖到浏览器页面**，松开即上传。

- 无文件大小限制
- 同名文件自动加数字后缀，不会覆盖

---

## 文件复制

1. 点击文件/文件夹右侧的 **📋** 按钮
2. 弹出**可视化目录选择器**，显示当前目录的子文件夹
3. 点击文件夹可进入，点击 `⬆ ..` 返回上级，点击顶部输入框可手动输入路径
4. 在目标目录点击 **"确认选择当前目录"**

同名时自动加 `_copy1`、`_copy2` 后缀。

---

## 文件移动

1. 点击文件/文件夹右侧的 **✂** 按钮
2. 弹出**可视化目录选择器**（同复制操作的界面）
3. 浏览到目标目录后点击确认

目标存在同名文件时会提示冲突。不能将文件夹移动到自身或其子目录中。

---

## 新建文件夹 / 文件

- **文件夹**：工具栏 → "📁+ 文件夹" → 输入名称
- **文件**：工具栏 → "📄+ 文件" → 输入文件名（如 `notes.md`），可选填初始内容

文件名不能包含 `\ / : * ? " < > |` 等特殊字符。

---

## 重命名

点击文件/文件夹右侧的 **✏** 按钮 → 输入新名称 → 回车确认。

---

## 删除文件

点击 **🗑** 按钮 → 确认弹窗 → 删除。

- **文件**：直接删除
- **空文件夹**：直接删除
- **非空文件夹**：弹出二次确认（红色警告），确认后递归删除全部内容

> 删除不经过回收站，永久删除，请谨慎操作。系统关键目录（如 `C:\Windows`、`/usr`）受保护，无法删除。

---

## 临时分享链接

1. 预览任意文件时，点击弹窗顶部的 **"🔗 分享"** 按钮
2. 自动生成一个临时链接（1 小时有效）
3. 点击"复制链接"发送给他人
4. 对方打开链接即可直接下载，**无需登录**

> 链接过期后自动失效，返回 410 Gone。

---

## 共享剪贴板

工具栏点击 **"📋 剪贴板"** → 输入文本 → 保存。在另一台设备上打开剪贴板即可读取。

适用场景：手机和电脑间传递网址、密码、代码片段等文本。

---

## 常用目录收藏

- **添加**：进入目录后点击工具栏 **"+⭐"**
- **查看**：点击 **"⭐ 收藏"** → 点击目录名跳转
- **删除**：收藏列表中点击 ✕

收藏数据持久保存在 `bookmarks.json` 中，重启不丢失。

---

## 排序与筛选

### 排序

| 按钮 | 默认方向 | 说明 |
|------|---------|------|
| 名称 | A→Z | 字母顺序 |
| 大小 | 小→大 | 文件大小 |
| 创建时间 | 旧→新 | 按创建时间 |
| 修改时间 | 新→旧 | 按修改时间 |

点击已激活的按钮可切换升/降序，文件夹始终排在最前面。

### 按类型筛选

下拉框选择：图片 / 视频 / 音频 / Markdown / 文本代码 / PDF / 压缩包 / Office / 字体 / 其他。

### 按扩展名筛选

输入框填写扩展名，如 `.py,.js,.ts`（逗号分隔），回车生效。两种筛选可叠加。

---

## 亮色 / 暗色主题

工具栏点击 **☀ / 🌙** 按钮切换。偏好自动保存到浏览器，下次打开自动恢复。

---

## 网格 / 列表视图

工具栏点击 **✦** 按钮切换：
- **列表视图**：传统文件列表（默认）
- **网格视图**：卡片式布局，图片文件自动显示缩略图

偏好自动保存。

---

## 中英语言切换

工具栏点击 **中 / EN** 按钮切换界面语言，偏好自动保存。

---

## 阻止系统睡眠

服务运行期间，程序默认会阻止电脑进入睡眠/休眠状态，确保手机等设备能持续访问。

**跨平台实现：**

| 平台 | 技术方案 | 说明 |
|------|---------|------|
| Windows | `SetThreadExecutionState` API | 通知系统有程序需要持续运行 |
| macOS | `caffeinate -i -w <pid>` | 系统自带命令，阻止空闲睡眠 |
| Linux | `systemd-inhibit --what=idle` | systemd 抑制锁机制 |

**行为：**
- 启动后终端显示 `Sleep: blocked` 表示生效
- 程序退出时（Ctrl+C 或关闭终端）自动恢复系统正常睡眠行为
- 不影响手动合盖睡眠或手动点击"睡眠"按钮

**关闭此功能：**
- 命令行：`python file_browser.py --no-sleep`
- 或修改文件：`PREVENT_SLEEP = False`

---

## 公网访问（外网穿透）

默认情况下，服务只能在局域网内访问。如果你需要从外网（如在外面用手机访问宿舍电脑）访问，可以使用以下内网穿透工具。

> **安全提醒**：公网访问意味着任何人都可能尝试连接你的服务。请务必：
> - 保持密码保护开启（不要设置 `PASSWORD = ""`）
> - 配置 `ALLOWED_ROOTS` 白名单限制可访问目录
> - 用完后立即关闭穿透服务

### 方案一：Cloudflare Tunnel（推荐）

免费、自带 HTTPS、无需注册、速度快。

**第 1 步：安装 cloudflared**

- Windows: 下载 https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/ 并放到 PATH 中
- macOS: `brew install cloudflared`
- Linux: `sudo apt install cloudflared` 或从官网下载

**第 2 步：启动 File Browser**

```bash
python file_browser.py
```

**第 3 步：另开一个终端，创建隧道**

```bash
cloudflared tunnel --url http://localhost:25600
```

终端会输出一个公网地址，类似：

```
+----------------------------+
|  https://xxx-yyy.trycloudflare.com  |
+----------------------------+
```

将这个地址发给自己，在任何地方的浏览器中打开即可访问，自带 HTTPS 加密。

**关闭**：在隧道终端按 `Ctrl+C` 即可停止公网访问，File Browser 本身不受影响。

### 方案二：ngrok

业界最流行的穿透工具，免费版有带宽限制。

**第 1 步：注册并安装**

1. 访问 https://ngrok.com/ 注册免费账号
2. 下载 ngrok 并按官网指引配置 authtoken

**第 2 步：启动隧道**

```bash
ngrok http 25600
```

终端显示公网地址（如 `https://xxxx.ngrok-free.app`），复制使用即可。

### 方案三：SSH 隧道（零安装）

利用系统自带的 SSH 连接免费中转服务，无需安装任何额外工具。

```bash
ssh -R 80:localhost:25600 nokey@localhost.run
```

终端会输出一个 `https://xxxx.localhost.run` 地址。

> 稳定性一般，适合临时使用。Windows 10 及以上自带 SSH 客户端。

### 方案对比

| 方案 | 安装 | 注册 | HTTPS | 免费 | 稳定性 | 速度 |
|------|------|------|-------|------|--------|------|
| **Cloudflare Tunnel** | 需安装 cloudflared | 不需要 | 自带 | 完全免费 | 高 | 快 |
| **ngrok** | 需安装 ngrok | 需注册 | 自带 | 有限制 | 高 | 快 |
| **SSH 隧道** | 不需要 | 不需要 | 自带 | 完全免费 | 一般 | 一般 |

---

## 访问日志

所有操作自动记录到 `access.log`，同时在终端实时显示：

```
2026-03-05 14:30:00 | 192.168.1.50 | LOGIN | success
2026-03-05 14:31:00 | 192.168.1.50 | DOWNLOAD | C:\Users\report.pdf
2026-03-05 14:32:00 | 192.168.1.50 | UPLOAD | C:\Users\photo.jpg
2026-03-05 14:33:00 | 192.168.1.50 | COPY | C:\a.txt -> C:\backup\a.txt
2026-03-05 14:34:00 | 192.168.1.50 | SHARE_CREATE | C:\Users\doc.pdf
```

记录的操作类型：LOGIN, BROWSE, PREVIEW, RAW, SEARCH, CONTENT_SEARCH, DOWNLOAD, DOWNLOAD_FOLDER, BATCH_DOWNLOAD, UPLOAD, MKDIR, MKFILE, DELETE, DELETE_RECURSIVE, RENAME, EDIT, COPY, MOVE, EXTRACT, SHARE_CREATE, SHARE_DOWNLOAD, CLIPBOARD, BOOKMARK_ADD, BOOKMARK_DEL, FOLDER_SIZE, ZIP_LIST。

---

## 支持的文件类型

### 可在线预览

| 类型 | 扩展名 |
|------|--------|
| 图片 | `.jpg` `.jpeg` `.png` `.gif` `.bmp` `.webp` `.svg` `.ico` |
| 视频 | `.mp4` `.webm` `.mkv` `.avi` `.mov` `.flv` `.wmv` |
| 音频 | `.mp3` `.wav` `.ogg` `.flac` `.aac` `.wma` `.m4a` |
| Markdown | `.md` `.markdown` `.mdown` `.mkd` |
| PDF | `.pdf` |
| Office | `.docx`（渲染为 HTML）`.xlsx` `.xls`（渲染为表格） |
| 压缩包 | `.zip`（查看内容列表 + 解压） |
| 文本/代码 | `.txt` `.py` `.js` `.ts` `.jsx` `.tsx` `.html` `.css` `.json` `.xml` `.yaml` `.yml` `.toml` `.ini` `.cfg` `.conf` `.sh` `.bash` `.bat` `.cmd` `.ps1` `.java` `.c` `.cpp` `.h` `.hpp` `.cs` `.go` `.rs` `.rb` `.php` `.sql` `.r` `.swift` `.kt` `.scala` `.lua` `.pl` `.env` `.gitignore` `.dockerfile` `.vue` `.svelte` `.log` `.csv` `.tsv` |

### 仅下载

| 类型 | 扩展名 |
|------|--------|
| 压缩包 | `.rar` `.7z` `.tar` `.gz` `.bz2` `.xz` |
| Office | `.doc` `.docx` `.xls` `.xlsx` `.ppt` `.pptx` `.odt` `.ods` `.odp` |
| 字体 | `.ttf` `.otf` `.woff` `.woff2` |

---

## 技术架构

```
┌──────────────────────────────────────────────────┐
│                  浏览器（手机/电脑）                 │
│  ┌────────────────────────────────────────────┐  │
│  │  HTML + CSS + JavaScript (SPA)             │  │
│  │  ├── marked.js       Markdown 渲染         │  │
│  │  ├── highlight.js    代码语法高亮           │  │
│  │  ├── mermaid.js      图表渲染              │  │
│  │  └── qrcode.js       二维码生成            │  │
│  └─────────────────────┬──────────────────────┘  │
└────────────────────────┼─────────────────────────┘
                         │ HTTP + Cookie 认证
┌────────────────────────┼─────────────────────────┐
│       Python Flask 服务 (0.0.0.0:25600)          │
│  ┌─────────────────────┴──────────────────────┐  │
│  │  file_browser.py (单文件，全部逻辑)         │  │
│  │                                             │  │
│  │  [认证] POST /api/login  GET /api/check-auth│  │
│  │  [浏览] /api/list /api/drives /api/info     │  │
│  │  [搜索] /api/search /api/search-content     │  │
│  │  [预览] /api/file /api/raw /api/zip-list    │  │
│  │  [下载] /api/download /api/download-folder  │  │
│  │  [管理] /api/upload /api/mkdir /api/delete  │  │
│  │         /api/rename /api/copy /api/move     │  │
│  │         /api/extract /api/save-file         │  │
│  │  [分享] /api/share  /share/<token>          │  │
│  │  [其他] /api/clipboard /api/bookmarks       │  │
│  │         /api/folder-size                    │  │
│  └─────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

**设计理念**：单文件部署、零配置启动、最小依赖、完全跨平台。

> 详细的 API 接口文档见 [API.md](API.md)。
